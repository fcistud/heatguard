# Health probe fixtures (WO-012)

- `valid/` — minimal data directory that satisfies hard readiness dependencies.
- `broken/` — missing `locales.json` and malformed `datasets.json` for not_ready paths.
